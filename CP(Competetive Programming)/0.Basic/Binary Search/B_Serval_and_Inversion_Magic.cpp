#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
int main()
{
    int t;
    cin >> t;
    for (int i = 0; i < t; i++)
    {
        int n, size;
        cin >> n;
        string s;
        cin >> s;
        if (n && 1)
        {
            size = n / 2;
        }
        else
        {
            size = (n / 2) - 1;
        }
    }
    return 0;
}