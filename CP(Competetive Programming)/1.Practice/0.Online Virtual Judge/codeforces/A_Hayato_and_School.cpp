#include <bits/stdc++.h>
using namespace std;
int main()
{
    int t, n;
    cin >> t;
    for (int i = 0; i < t; i++)
    {
        cin >> n;
        int val;
        cin >> val;
        vector<int> vec;
        vec.push_back(val);
        pair<int, vector<int>> pair_of_vec;
        for (int j = 0; j < n; j++)
        {
            pair_of_vec.push_back({})
        }
    }
    return 0;
}
