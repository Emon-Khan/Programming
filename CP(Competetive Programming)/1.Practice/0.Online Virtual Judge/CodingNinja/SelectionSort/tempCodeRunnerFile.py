for i in range(n):
    #     val = arr[i]
    #     index = i
    #     for j in range(i+1, n):
    #         if arr[i] > arr[j]:
    #             val = min(val, arr[j])
    #             if index != arr.index(val):
    #                 index = arr.index(val)
    #             else:
    #                 index = j
    #         elif (arr[i] == arr[j]):
    #             arr[j], arr[i+1] = arr[i+1], arr[j]
    #     if i != index:
    #         arr[i], arr[index] = arr[index], arr[i]